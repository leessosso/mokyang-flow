rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 본 앱은 firebase-admin 서버만 접근한다. 클라이언트 직접 업로드는 막는다.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
